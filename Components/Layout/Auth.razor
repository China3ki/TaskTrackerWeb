<div class="auth__wrapper">
    <div class="auth">
        <div class="auth__header">
            <span class="auth__close" @onclick="CloseForm"></span>
            <div class="auth__select">
                <button class="auth__option @(AuthOption == AuthModel.Login ? "show" : "")" @onclick="() => HandleFormModel(AuthModel.Login)">Login</button>
                <button class="auth__option @(AuthOption == AuthModel.Register ? "show" : "")" @onclick="() => HandleFormModel(AuthModel.Register)">Register</button>        
            </div>
        </div>
        <div class="auth__content ">
            @if(AuthOption == AuthModel.Login)
            {
                <div class="auth__info login">
                    <h1 class="auth__title">Login</h1>
                    <span class="auth__desc">Hi! Welcome back!</span>
                </div>
                <form class="auth__form login">
                    <div class="auth__pair">
                        <label class="auth__label auth__label--email" for="email"></label>
                        <input class="auth__input" id="email" type="email" placeholder="Email" />
                    </div>
                    <div class="auth__pair">
                        <label class="auth__label auth__label--password" for="password"></label>
                        <input class="auth__input" id="password" type="password" placeholder="Password" />
                    </div>
                    <div class="auth__pair">
                        <input class="auth__input auth__input--submit" type="submit" value="Login" />
                    </div>
                    @*Wyświetlić komunikat!*@
                    @if (!string.IsNullOrEmpty(RegisterSuccess) && RegisterSuccess == "true")
                    {
                        <div class="auth__success">
                            <span class="auth__success--info">Your account has been created!</span>
                            <span>Now you can login</span>
                        </div>
                    }
                </form>
            }
            else
            {
                <div class="auth__info">
                    <h1 class="auth__title">Register</h1>
                    <span class="auth__desc">Hi! Nice to meet you!</span>
                </div>
                <EditForm class="auth__form" EditContext="@_editContext" OnValidSubmit="SubmitRegister">
                    <DataAnnotationsValidator/> 
                    <div class="auth__pair">
                        <label class="auth__label auth__label--user" for="name"></label>
                        <InputText class="auth__input" id="name" placeholder="Name" @bind-Value="_register.Name"/>
                    </div>
                    <div class="auth__pair">
                        <label class="auth__label auth__label--user" for="surname"></label>
                        <InputText class="auth__input" id="surname" placeholder="Surname" @bind-Value="_register.Surname" />
                    </div>
                    <div class="auth__pair">
                        <label class="auth__label auth__label--email" for="email"></label>
                        <InputText class="auth__input" id="email" type="email" placeholder="Email" @bind-Value="_register.Email" />
                    </div>
                    <div class="auth__pair">
                        <label class="auth__label auth__label--password" for="password"></label>
                        <InputText class="auth__input" id="password" type="password" placeholder="Password" @bind-Value="_register.Password" />
                    </div>
                    <div class="auth__pair">
                        <label class="auth__label auth__label--password" for="confirmed"></label>
                        <InputText class="auth__input" id="confirmed" type="password" placeholder="Confirmed Password" @bind-Value="_register.ConfirmedPassword" />
                    </div>
                    <div class="auth__pair">
                        <input class="auth__input auth__input--submit" type="submit" value="Register" />
                    </div>
                    <ValidationSummary />
                </EditForm>
                <div class="auth__validation">
                    <h2 class="auth__head">Password requirements:</h2>
                    <ul class="auth__list">
                        <li>Atleast 8 characters</li>
                        <li>Atleast one upper case</li>
                        <li>Atleast one number</li>
                        <li>Atleast one special character</li>
                    </ul>
                </div>
            }          
        </div>
    </div>
</div>